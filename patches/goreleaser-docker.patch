diff --git a/tools/scripts/internal/goreleaser/project.go b/tools/scripts/internal/goreleaser/project.go
--- a/tools/scripts/internal/goreleaser/project.go
+++ b/tools/scripts/internal/goreleaser/project.go
@@ -32,6 +32,25 @@
 		project.Archives = append(project.Archives, a.Archives(archiveBuilds)...)
 	}
 	project.UniversalBinaries = universalBinaries(project.Builds)
+	project.DockersV2 = []config.DockerV2{
+		{
+			ID:         "server",
+			IDs:        []string{"server_linux_amd64", "server_linux_arm64"},
+			Dockerfile: "tools/server-docker/server/Dockerfile",
+			Images: []string{
+				"ghcr.io/tofuliang/agelanserver",
+			},
+			Tags: []string{
+				"{{ .Version }}",
+				"latest",
+			},
+			ExtraFiles: []string{
+				"server/resources/config",
+				"server/resources/responses",
+			},
+			Platforms: []string{"linux/amd64", "linux/arm64"},
+		},
+	}
 
 	project.Checksum = config.Checksum{
 		NameTemplate: `{{ .ProjectName }}_{{ .RawVersion }}_checksums.txt`,
diff --git a/tools/server-docker/server/Dockerfile b/tools/server-docker/server/Dockerfile
--- a/tools/server-docker/server/Dockerfile
+++ b/tools/server-docker/server/Dockerfile
@@ -1,28 +1,12 @@
 # syntax=docker/dockerfile:1
 
-# Compile
-FROM golang:1.26-alpine3.23 AS compiler
-WORKDIR /app
-COPY server-docker/server/go.work.template go.work
-COPY common common
-COPY server server
-RUN mkdir build
-RUN cp -r server/resources build/resources && rm -rf build/resources/windows && rm -rf build/resources/unix
-RUN mkdir -p build/resources/certificates
-RUN go build -ldflags="-s -w" -o build/server ./server
-# Compress
-FROM alpine:3.23 AS compressor
-RUN apk add --no-cache upx
-WORKDIR /app
-COPY --from=compiler /app/build/server .
-RUN upx --lzma server
-# Execute
 FROM scratch
 EXPOSE 443
 VOLUME /app/resources/config
 VOLUME /app/resources/certificates
 VOLUME /app/logs
 WORKDIR /app
-COPY --from=compiler /app/build/resources resources
-COPY --from=compressor /app/server .
-ENTRYPOINT ["./server"]
\ No newline at end of file
+COPY server/resources/config resources/config
+COPY server/resources/responses resources/responses
+COPY server ./server
+ENTRYPOINT ["./server"]
